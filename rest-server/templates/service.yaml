apiVersion: v1
kind: Service
metadata:
  name: {{ include "rest-server.fullname" . }}
  labels:
    {{- include "rest-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "rest-server.selectorLabels" . | nindent 4 }}
